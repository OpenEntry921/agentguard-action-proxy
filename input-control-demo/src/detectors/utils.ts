import {createHash} from 'node:crypto';
import {Decision,Finding,Severity} from '../models.js';
export function add(out:Finding[],type:string,category:string,severity:Severity,confidence:number,value:string,start:number,source:string,rule:string,extra:Partial<Finding>={}){out.push({category,severity,type,matchedRule:rule,evidence:mask(type,value),maskedValue:mask(type,value),originalValueStored:false,fingerprint:fingerprint(value),location:{source,start,end:start+value.length},confidence,...extra})}
export function fingerprint(v:string){return createHash('sha256').update(v).digest('hex').slice(0,16)}
export function mask(type:string,v:string){if(/DATABASE|POSTGRES|MYSQL|MONGO|REDIS|JDBC/.test(type))return v.replace(/:\/\/([^:]+):([^@]+)@/,'://***:***@').replace(/\/[^/?\s]+/,'/***');if(type==='XRPL_FAMILY_SEED')return v.slice(0,3)+'**************************';if(/SECRET|TOKEN|KEY|PRIVATE|COOKIE|CREDENTIAL|AWS|OPENAI|ANTHROPIC|SLACK|STRIPE|GITHUB|GITLAB/.test(type))return (v.slice(0,4)||'****')+'************';if(type==='BUSINESS_REGISTRATION_NUMBER')return '***-**-*****';if(type.includes('CARD'))return '****-****-****-'+v.replace(/\D/g,'').slice(-4);if(type.includes('WALLET')||type.includes('ADDRESS'))return v.slice(0,6)+'…'+v.slice(-4);if(type==='PERSON_NAME')return v[0]+'**';if(type==='DATE_OF_BIRTH')return '****년 **월 **일';return v.length<=4?'****':v.slice(0,2)+'*'.repeat(Math.min(12,v.length-4))+v.slice(-2)}
export const rank:Record<Decision,number>={ALLOW:0,MASK:1,REVIEW:2,BLOCK:3};
