import {Finding,MaskingResult} from './models.js';import {detectSensitiveData,maskValue} from './sensitive-data-detector.js';
export function maskText(text:string,findings:Finding[]):MaskingResult{const maskable=findings.filter(f=>f.category==='SENSITIVE_DATA'&&!['PRIVATE_KEY','API_KEY','ACCESS_TOKEN','BEARER_TOKEN','JWT','PASSWORD_SECRET','CLOUD_CREDENTIAL'].includes(f.type||''));let safe=text;for(const f of [...maskable].sort((a,b)=>b.location.start-a.location.start)){const raw=safe.slice(f.location.start,f.location.end);safe=safe.slice(0,f.location.start)+maskValue(f.type||'',raw)+safe.slice(f.location.end)}const residual=detectSensitiveData(safe).filter(f=>!['EMAIL','PHONE','KR_RRN','CREDIT_CARD','IP_ADDRESS'].includes(f.type||''));return{originalRetained:false,maskingApplied:maskable.length>0,maskedTypes:[...new Set(maskable.map(f=>f.type||'UNKNOWN'))],maskedCount:maskable.length,safeText:safe,residualFindings:residual}}
