import {Finding} from './models.js';
const rules=[['ignore_previous_instructions',/ignore all previous instructions|이전(의)? 모든 지시.*무시|보안정책.*무시/i,'HIGH'],['system_prompt_disclosure',/system prompt|시스템 프롬프트|developer message|저장된 비밀정보/i,'HIGH'],['exfiltrate_url',/https?:\/\/\S+.*(send|upload|전송|업로드)|attacker server|공격자 서버/i,'HIGH'],['credential_collection',/(api key|token|password|secret|비밀번호|토큰|키).*(collect|수집|출력|보내)/i,'HIGH'],['hidden_instruction',/hidden instruction|문서에 숨겨진 지시|follow.*document instruction/i,'MEDIUM'],['obfuscation_bypass',/base64|rot13|인코딩|난독화/i,'MEDIUM'],['tool_poisoning',/tool result.*trust|도구 결과.*신뢰/i,'MEDIUM'],['unsafe_execution',/(without approval|승인 없이).*(delete|deploy|transfer|삭제|배포|송금)/i,'HIGH'] ] as const;
export function scanPrompt(text:string,source='prompt'):Finding[]{const out:Finding[]=[];for(const[r,re,sev]of rules){const m=re.exec(text);if(m?.index!==undefined)out.push({category:'PROMPT_INJECTION',severity:sev,type:'PROMPT_INJECTION',matchedRule:r,evidence:text.slice(m.index,Math.min(m.index+m[0].length, m.index+80)),location:{source,start:m.index,end:m.index+m[0].length},confidence:.9})}return out}
